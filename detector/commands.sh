#!/usr/bin/sh

# to extract the .tgz files
# for file in *.tgz; do tar -zxf "$file"; done 

# to convert a npy files to tgz, run this command in the terminal(linux)
# for file in *.npy; do tar -czf "$file.tgz" "$file"; done
